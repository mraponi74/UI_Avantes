# Copy this file to .env and adjust as needed:
#   cp .env.example .env
#
# Host port to publish the web UI on. If no .env file is present,
# docker-compose.yml falls back to 8000 on its own.
HOST_PORT=8000
